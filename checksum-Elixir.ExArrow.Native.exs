# This file is populated by running:
#
#   mix rustler_precompiled.download ExArrow.Native --all
#
# after the GitHub release for the current version has been published and
# precompiled NIF binaries have been uploaded as release assets.
#
# During local development the NIF is always built from source
# (config/config.exs sets `force_build: true` for dev/test), so this file
# is not needed for day-to-day work.
%{
}
